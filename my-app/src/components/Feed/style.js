import { StyleSheet } from "react-native";

const feedStyle = StyleSheet.create({
  feedStyle: {
    height: "120%",
  },
  flatListContent: {
    paddingBottom: 20,
  },
});
export default feedStyle;
